/// §20.3: a single deterministic, locally-computed observation, e.g. "Your
/// best day for finishing tasks is Tuesday." Never a judgement, always a
/// fact the numbers already show.
class Insight {
  const Insight(this.text);

  final String text;
}
